page('/',
  controller.landing);

page('/login',
  controller.login);

page('/register',
  controller.register);

page('/home',
  controller.homepage);

page('/about',
  controller.about);

page('/resources',
  controller.resources);

page();
